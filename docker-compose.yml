services:
  larder:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - larder-data:/data
    environment:
      - DATABASE_URL=sqlite:/data/larder.db
      - LARDER_JWT_SECRET=${LARDER_JWT_SECRET:-change-me-set-in-env}
    restart: unless-stopped

volumes:
  larder-data:
